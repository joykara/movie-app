'use client';
import LoginForm from "./Login";

export default function LoginPage() {
    return (
        <div className="flex items-center justify-center h-[80vh] w-full bg-white dark:bg-black">
            <LoginForm />
        </div>
    )
}